# Family (example)

Household-level facts for Bernie (routines, preferences, house rules).

- Quiet by default; high-signal interruptions only.
- Confirm high-impact actions (group DMs, device changes that affect everyone).
- Timezone and channel IDs live in `config.json`, not here.

*Generic pack for public export. Private deploys keep real docs under `docs/` or gitignored private paths.*
