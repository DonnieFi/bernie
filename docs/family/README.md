# Generic family pack (public export)

These files are **placeholders** for the open-source tree (`mom` / `dad` / `child1` / `child2`).

Private monorepo deploys may keep real person docs separately. The public GitHub tree ships only this generic pack.

Map person keys in `config.json` → person markdown filenames as you prefer.
